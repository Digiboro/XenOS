# Cargo configuration for XenOS

[alias]
# xtask pattern - запуск build system
xtask = "run --package xtask --"

[build]
# Переопределение линкера для custom targets (если нужно)
# rustflags = ["-C", "link-arg=-fuse-ld=lld"]

[target.x86_64-unknown-uefi]
# UEFI target не требует особых настроек
